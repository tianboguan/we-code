syntax = "proto3";

service Record {
  // Sends a greeting
  rpc HandleRecord (RecordReq) returns (RecordRes) {}
}

message RecordReq {
  string id = 1;
  int64 time = 2;
}

message RecordRes {
  int32 code = 1;
  string id = 2;
}
